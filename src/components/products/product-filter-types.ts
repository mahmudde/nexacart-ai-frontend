export type ProductFilters = {
  search: string;
  categoryId: string;
  brand: string;
  minPrice: string;
  maxPrice: string;
  minRating: string;
  sort: string;
  page: number;
  limit: number;
};
